import { WarningsList } from '@/components/Pages/warnings'
import { Layout } from '@/Layouts'

const WarningsListPage = () => {
  return (
    <Layout>
      <WarningsList />
    </Layout>
  )
}

export default WarningsListPage