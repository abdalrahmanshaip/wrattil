import { Quran } from "@/components/Pages/home"
import { Layout } from "@/Layouts"

const HomePage = () => {
  return (
   <Layout>
      <Quran />
   </Layout>
  )
}

export default HomePage